#include <stdio.h>
int main() {
    int num;
    printf("Enter num : ");
    scanf("%d",&num);
    // calculate square of number
    int square = num*num;
    printf("suare of num is : %d\n",square);
    return 0;
}